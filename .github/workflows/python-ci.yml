name: Python CI (Data Pipeline)

on:
  push:
    branches: [main, master, develop]
    paths:
      - 'data-pipeline/**'
      - '.github/workflows/python-ci.yml'
  pull_request:
    branches: [main, master, develop]
    paths:
      - 'data-pipeline/**'
      - '.github/workflows/python-ci.yml'

# NOTE: test_aggregate_kpis.py requires PySpark and is excluded from CI.
# Run it locally with: pip install pyspark && pytest data-pipeline/tests/test_aggregate_kpis.py

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: data-pipeline

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install runtime dependencies
        run: pip install -r requirements.txt

      - name: Install dev dependencies
        run: pip install flake8 pytest pytest-cov

      - name: Lint with flake8
        run: |
          # Fail on syntax errors and undefined names
          flake8 kafka_to_minio.py spark_jobs/aggregate_sensor_kpis.py spark_jobs/streaming_anomaly_detector.py \
            --count --select=E9,F63,F7,F82 --show-source --statistics
          # Style warnings only (max-line-length=120, does not fail the build)
          flake8 kafka_to_minio.py spark_jobs/aggregate_sensor_kpis.py spark_jobs/streaming_anomaly_detector.py \
            --count --max-line-length=120 --statistics --exit-zero

      - name: Run unit tests with coverage
        run: |
          python -m pytest tests/test_kafka_to_minio.py tests/test_anomaly_detection.py \
            -v --tb=short \
            --cov=. \
            --cov-report=xml:coverage.xml \
            --cov-report=term-missing \
            --ignore=tests/test_aggregate_kpis.py

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: python-coverage
          path: data-pipeline/coverage.xml
          retention-days: 14
