# AquaFlow data-pipeline dependencies
# Pinned for reproducible builds

kafka-python==2.0.2          # Pure-Python Kafka client (no native deps)
pyarrow==15.0.2              # Parquet read/write + columnar format
boto3==1.34.69               # AWS S3 SDK — used for MinIO (S3-compatible) — pinned, see dependabot.yml
botocore==1.34.69            # boto3 dependency (pinned to match boto3) — do not bump independently
